<%=
  config = {
    username: p("username"),
    password: p("password"),
    port: p("port"),
    deployer_path: "/var/vcap/packages/blockhead/deployer/pusher.js",
    container_manager: "docker",
  }

  config.to_json
%>
