<%=
  config = {
    username: p("username"),
    password: p("password"),
    port: p("port"),
    deployer_path: "/var/vcap/packages/blockhead/deployer/pusher.js",
    container_manager: "docker",
    external_address: spec.ip,
  }

  config.to_json
%>
